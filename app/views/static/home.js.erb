var content = $("<%=j render 'feed' %>");
console.log(content)
window.added=content
$(".item-list").html(content);
$(".shots-wrap .next-wrap").replaceWith($(".next-wrap",content));
window.pckry.addItems(content);
